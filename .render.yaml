services:
  - type: web
    name: budget-blaze-blitz
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn main.wsgi:application
